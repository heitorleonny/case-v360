<p class="text-lg text-white mb-4">Hello <span class="font-semibold text-blue-600"><%= @email %></span>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p class="text-md text-white mb-6">We're contacting you to notify you that your email is being changed to <span class="font-semibold text-blue-600"><%= @resource.unconfirmed_email %></span>.</p>
<% else %>
  <p class="text-md text-white mb-6">We're contacting you to notify you that your email has been changed to <span class="font-semibold text-blue-600"><%= @resource.email %></span>.</p>
<% end %>

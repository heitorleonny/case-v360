<p class="text-lg text-white mb-4">Welcome <span class="font-semibold text-blue-600"><%= @email %></span>!</p>

<p class="text-md text-white mb-6">You can confirm your account email through the link below:</p>

<p class="text-center">
  <%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token), class: "inline-block bg-blue-600 text-white py-2 px-4 rounded-lg font-semibold shadow-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50" %>
</p>
